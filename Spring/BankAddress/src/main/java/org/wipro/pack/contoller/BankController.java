package org.wipro.pack.contoller;



import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import org.wipro.pack.beans.Address;
import org.wipro.pack.beans.BankDetails;
import org.wipro.pack.repo.BankRepository;

//@RestController
//@RequestMapping("/api")
public class BankController {
//    @Autowired
//    private BankRepository bankRepository;
//
//    @GetMapping("/bank/name")
//    public String getBankName() {
//        BankDetails bank = bankRepository.findById(1L).orElse(null);
//        return (bank != null) ? bank.getName() : "Bank Not Found";
//    }
//
//    @GetMapping("/bank/address")
//    public Address getBankAddress() {
//        BankDetails bank = bankRepository.findById(1L).orElse(null);
//        return (bank != null) ? bank.getAddress() : null;
//    }
}


